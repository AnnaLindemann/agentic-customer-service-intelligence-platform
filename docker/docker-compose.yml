# Phase 1 Compose file.
#
# Builds and runs the Express backend for local development and the Oracle Cloud
# target environment. Exposes GET /health on port 3000.

services:
  app:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    env_file:
      - ../.env
    environment:
      - NODE_ENV=production
      - PORT=3000
    ports:
      - "3000:3000"
